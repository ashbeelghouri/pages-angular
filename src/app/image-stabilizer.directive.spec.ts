import { ImageStabilizerDirective } from './image-stabilizer.directive';

describe('ImageStabilizerDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageStabilizerDirective();
    expect(directive).toBeTruthy();
  });
});
